# Phaser game template (typescript, webpack)

Boilerplate project for creating new games based on Phaser, Typescript and Webpack.

## Run the game

`npm install`

`npm start`

## Run the game

`npm install`

`npm start`